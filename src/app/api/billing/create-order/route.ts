// Legacy alias — use /api/billing/create-subscription
export { /* @next-codemod-error `POST` export is re-exported. Check if this component uses `params` or `searchParams`*/
POST } from '../create-subscription/route'
